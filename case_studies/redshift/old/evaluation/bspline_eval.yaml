---
defaults:
    - ..@_here_: redshift_bspline
    - _self_
    # - override hydra/job_logging: stdout

paths:
    ckpt_dir: ${paths.data_dir}/checkpoints/bspline_split_0_2025-05-15-09-05-36/checkpoints
    # ckpt_dir: ${paths.data_dir}/checkpoints/bspline_split_0_2025-04-22-23-17-28/checkpoints
    plot_dir: ${paths.data_dir}/plots

# To reduce memory usage
surveys:
    dc2:
        batch_size: 4
        max_sources_per_tile: 5
