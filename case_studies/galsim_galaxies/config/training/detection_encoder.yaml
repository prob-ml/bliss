# @package _global_
training:
  name: "detection_encoder"
  model: ${models.detection_encoder}
  dataset: ${blends_datasets.blends_datamodule}
  n_epochs: 30
  trainer:
    enable_checkpointing: True
    check_val_every_n_epoch: 1
