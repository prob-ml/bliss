defaults:
  - optimizer: adam
  - model: simple_model

general:
  root: "."
  logs: "logs"
  overwrite: False
  output: "example"
  gpus: "2,"

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}_${hydra.job.override_dirname}
