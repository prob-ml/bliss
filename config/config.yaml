defaults:
  - optimizer: adam
  - model: simple_model

general:
  device: cuda
  root: .
  logs: logs
  overwrite: False
  output: ???


hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}_${hydra.job.override_dirname}
