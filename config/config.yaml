defaults:
  - optimizer: adam
  - model: basic_sleep_star
  - training: default
  - dataset: default

general:
  overwrite: False

paths:
  root: ${env:BLISS_HOME}
  output: logs/example

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}_${hydra.job.override_dirname}
