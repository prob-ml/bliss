defaults:
  - optimizer: adam
  - model: basic_sleep_star
  - training: default
  - dataset: default
  - testing: default
  - generate: default

general:
  overwrite: False

paths:
  root: ${env:BLISS_HOME}
  output: ${paths.root}/logs/example
