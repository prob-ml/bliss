# @package _group_
image:
  slen: 20
decoder:
  params:
    tile_slen: 4
    ptile_slen: 24
    prob_galaxy: 1.0
    max_sources: 2
    mean_sources: 0.12
    decoder_file: ${paths.root}/data/galaxy_decoder_1_band.dat
encoder:
  params:
    ptile_slen: 24
