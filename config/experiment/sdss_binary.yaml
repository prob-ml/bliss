# @package _global_
defaults:
  - override /model: binary_sdss
  - override /dataset: binary

mode: train

training:
  n_epochs: 501
  save_top_k: 5
  trainer:
    checkpoint_callback: true

dataset:
  prior:
    mean_sources: 0.03

optimizer:
  kwargs:
    lr: 1e-4
