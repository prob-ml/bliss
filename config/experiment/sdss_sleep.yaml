# @package _global_
defaults:
  - override /model: sleep_sdss_detection
  - override /dataset: default

mode: train

training:
  n_epochs: 1501
  save_top_k: 10
  trainer:
    checkpoint_callback: true

model:
  decoder:
    mean_sources: 0.03

optimizer:
  kwargs:
    lr: 1e-4
