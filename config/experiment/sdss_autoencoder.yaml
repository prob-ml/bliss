# @package _global_
defaults:
  - override /model: galaxy_net
  - override /dataset: sdss_galaxies
  - override /optimizer: adam

mode: train

training:
  n_epochs: 10001
  trainer:
    check_val_every_n_epoch: 10
    checkpoint_callback: true

dataset:
  num_workers: 5
